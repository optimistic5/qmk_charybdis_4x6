# Custom keymap logic (dragscroll on hold, auto pointer layer helpers)
SRC += source.c

# The keyboard ships with per-key RGB enabled by default; keep it off to save
# space and match the previous behaviour. Remove this line to enable RGB.
RGB_MATRIX_ENABLE = no
